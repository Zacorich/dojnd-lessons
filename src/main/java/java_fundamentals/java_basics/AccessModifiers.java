package java_fundamentals.java_basics;

public class AccessModifiers {
    //private - defining class only
    //default - defining class, same package classes
    //protected - defining class, same package classes, subclasses( if they lie in other packages)
    //public - defining class, same package classes, subclasses( if they lie in other packages), any classes from other packages
}
